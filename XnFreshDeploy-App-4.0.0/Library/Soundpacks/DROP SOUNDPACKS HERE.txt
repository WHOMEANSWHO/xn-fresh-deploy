Use Pack library inside Xn Fresh Deploy to import a folder or ZIP safely.

The folder name becomes the soundpack name by default. The app asks whether you want to rename it before the import completes.
