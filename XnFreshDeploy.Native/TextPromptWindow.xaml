<Window x:Class="XnFreshDeploy.TextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="400" Height="190" ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}" FontFamily="Segoe UI"
        WindowStyle="ToolWindow" ShowInTaskbar="False">
  <Border Style="{StaticResource Card}" Margin="14">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <TextBlock x:Name="PromptTitle" FontSize="16" FontWeight="SemiBold"/>
      <StackPanel Grid.Row="1" Margin="0,14,0,0">
        <TextBlock x:Name="PromptLabel" Style="{StaticResource FieldLabel}"/>
        <TextBox x:Name="ValueBox" Height="36"/>
      </StackPanel>
      <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
        <Button Content="Cancel" Style="{StaticResource TextButton}" IsCancel="True" Padding="12,6"/>
        <Button Content="Save" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0" IsDefault="True" Click="Save_Click"/>
      </StackPanel>
    </Grid>
  </Border>
</Window>
